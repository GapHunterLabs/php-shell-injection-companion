<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.phpshellinjectioncompanion</id>
    <name>PHP Shell Injection Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on <code>eval(</code>, <code>exec(</code>,
        <code>shell_exec(</code>, <code>system(</code>,
        <code>passthru(</code>, or <code>popen(</code> whose argument
        is a double-quoted string with <code>$variable</code>
        interpolation, or a concatenation with a variable, rather than
        a static literal. PHP's own official manual states this for
        <code>eval()</code>: "pay special attention not to pass any
        user provided data into it without properly validating it
        beforehand" -- the same class of risk applies to the
        shell-executing functions, all of which run their string
        argument through a real system shell.</p>
        <p>Confirmed real gap: "PHP Inspections (EA Extended)" (one of
        the most widely used PHP inspection plugins on Marketplace)
        covers <code>unserialize()</code>, weak crypto, SSL bypass, and
        several other security patterns, but not
        <code>eval()</code>/<code>exec()</code>/<code>shell_exec()</code>/
        <code>system()</code> with dynamic arguments -- confirmed by
        reading its own documented security feature list before
        building this.</p>
        <p><b>100% static text analysis</b> -- a regex-based line
        scanner, not a real PHP parser, so it works whether the PHP
        plugin is installed or not.</p>
        <p>Stated honestly: v0.1 doesn't trace whether the interpolated
        value actually originates from untrusted input, so
        interpolation of a hardcoded constant is a possible (rare)
        false positive. A call whose argument is entirely a static
        literal is correctly never flagged.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="PHP Shell Injection Companion"
            displayName="Interpolated/concatenated string passed to a dangerous PHP function"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.phpshellinjectioncompanion.inspection.ShellCommandInjectionInspection"/>
        <notificationGroup id="PHP Shell Injection Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
